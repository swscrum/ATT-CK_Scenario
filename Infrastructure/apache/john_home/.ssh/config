Host *
    StrictHostKeyChecking accept-new
    ServerAliveInterval 30

# This is John's deploy-side SSH config on the apache server.
# John's day-to-day machine is his workstation; this account exists
# so deploys can SSH in from the workstation, not the other way round.
