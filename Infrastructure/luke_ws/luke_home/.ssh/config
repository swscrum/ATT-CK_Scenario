Host *
    StrictHostKeyChecking accept-new
    ServerAliveInterval 30

# Apache web server (DMZ) — runs the Waystar Connect intranet.
Host apache
    HostName 10.40.0.2
    User luke.smith
    Port 22

# Patient database (no SSH access — use psql with ~/.pgpass).
Host db-internal
    HostName 10.30.0.6
