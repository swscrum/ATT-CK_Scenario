# Luke's accumulated host-key trust. Populated on first contact during
# his rsync backups to apache.
# Refreshed at container start by the entrypoint (ssh-keyscan against the
# live apache host) so the entry matches the actual host key.
