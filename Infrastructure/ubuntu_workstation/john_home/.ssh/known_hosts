# John's accumulated host-key trust. Populated on first contact during
# normal deploy work + onboarding attempts to internal hosts.
# Refreshed at container start by the entrypoint (ssh-keyscan against the
# live fleet) so entries match the actual host keys baked into each container.
