Host *
    StrictHostKeyChecking accept-new
    ServerAliveInterval 30
    IdentityFile ~/.ssh/id_ed25519

# Apache web/intranet server (DMZ).
Host apache
    HostName 10.40.0.2
    User vinzenz.fedora

# John Stravidis's workstation (frontend freelancer).
Host john
    HostName 10.30.0.5
    User vinzenz.fedora

# Luke Smith's workstation (psychiatrist).
Host luke
    HostName 10.30.0.7
    User vinzenz.fedora

# Patient database — no SSH; use psql via ~/.pgpass.
Host db-internal
    HostName 10.30.0.6
    # SSH not running on db container; psql only.
