# Vinzenz's accumulated host-key trust. Populated on first contact with each
# managed host during fleet onboarding; refreshed at container start by the
# entrypoint (ssh-keyscan against the live fleet) so the entries match the
# actual host keys baked into each container.
#
# Lines are unhashed (HashKnownHosts no) so cross-checking by hostname is
# grep-able during incident response.
#
# (db-internal has no sshd — tried connecting once on 2025-12-04; left a
# reminder so I don't try again.)
