# 2026-06-08 — on-call notes

Trigger: weekly patching window. Routine run.

```
ansible all -i ~/inventory.ini -m shell -a 'uname -r; uptime'
ansible all -i ~/inventory.ini -b -m shell -a 'apt update >/dev/null && apt list --upgradable 2>/dev/null'
```

## What got upgraded

- apache: libcap2-bin 1:2.44-1 → 1:2.44-2 (security, low CVSS)
- john: nodejs npm minor bump (deferred — not security)
- luke: postgresql-client-16 (security backport)
- vinzenz_ws: same postgresql-client + ansible 2.18.x → 2.18.y

Kernel: no changes this week.

## Time

Whole cycle took ~22 minutes including verification. Faster than usual
because no reboots needed.

## Followup

- The apache image is on httpd:2.4.50 still. We've talked about bumping
  to 2.4.62 (current). Postponed again — the booking CGI breaks on
  2.4.51+ because of how the upstream maintainers tightened mod_cgi
  path handling, and we haven't refactored. On the perpetual TODO.
